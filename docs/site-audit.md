# Website review and improvement backlog

Reviewed September 18, 2026 on desktop and mobile.

## Fixed in the approval preview

- Replaced generic imagery with two original student–company scenes that show mentoring, project review and a sponsor conversation.
- Added a thin scroll progress bar, an opening scroll cue, moving ambient light and subtle section reveals.
- Added a student/company story section to both public pages to break up the long directory scroll.
- Reworded application claims so the preview does not imply that disabled delivery is already live.
- Replaced the hover-only card instruction with an action that also makes sense on touch devices.
- Preserved reduced-motion behavior, keyboard focus styles and the existing responsive layout.

## Features that still need implementation

1. Connect the structured application flow to durable storage, sponsor delivery and applicant confirmation. Until then, submission remains disabled.
2. Replace the static sponsor export with a server-only refresh process and show a visible last-checked date on every listing.
3. Add authentication and saved team profiles so teams do not re-enter the same information.
4. Build moderator review for company claims, applications, expired records and corrections.
5. Add company dashboards only after ownership verification and role permissions are defined.
6. Add real partner content when a formal relationship exists; the current empty state is intentionally honest.
7. Improve logo quality and cache external sponsor assets so the directory is less dependent on third-party image hosts.
8. Add analytics for search terms, filter use, profile opens and official-program clicks without collecting private application content.

## Suggested next improvements

- Add sponsor success stories with a measurable project outcome once real teams and companies provide permission.
- Add saved searches and deadline reminders after sign-in exists.
- Add compact and expanded directory views for visitors comparing many programs.
- Add related sponsors inside the detail dialog based on support type and team eligibility.
- Add shareable filtered URLs so a team can send a curated sponsor shortlist.
- Convert the new PNG photography to modern responsive image formats during deployment for faster first load.
